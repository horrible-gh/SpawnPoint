# SpawnPoint 저장소 계층은 sqloader(SQLite 백엔드)를 사용한다.
# 그 외 모듈은 Python 표준 라이브러리만 사용한다.
# 설치: pip install -r requirements.txt
# 테스트도 표준 unittest 로 실행한다:
#   python -m unittest discover -s tests -v
sqloader==0.2.18
